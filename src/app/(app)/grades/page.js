import { ModulePlaceholder } from "@/components/layout/module-placeholder";

export default function GradesPage() {
  return (
    <ModulePlaceholder
      title="Notes & bulletins"
      description="Saisie des notes par matière/enseignant, coefficients, moyennes automatiques, bulletins PDF, classement de classe."
      specRef="§5.2 Notes & bulletins"
    />
  );
}
